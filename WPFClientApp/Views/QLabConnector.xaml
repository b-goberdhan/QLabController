<UserControl x:Class="WPFClientApp.Views.QLabConnector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFClientApp.Views"
             xmlns:converter="clr-namespace:WPFClientApp.Helpers.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Width="400" Height="400" Background="White">
        <Grid Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisConverter}}">
            <TextBlock VerticalAlignment="Top" HorizontalAlignment="Center" 
                   FontSize="40" Margin="0,40,0,0">Connecting To QLab...</TextBlock>
        </Grid>
        <Grid Visibility="{Binding IsNotConnected, Converter={StaticResource BoolToVisConverter}, FallbackValue=Collapsed}">
            <TextBlock VerticalAlignment="Top" HorizontalAlignment="Center"
                       FontSize="40" Margin="0,40,0,0">Connected!</TextBlock>
        </Grid>
        <Button Width="100" Height="50" VerticalAlignment="Bottom" Margin="0,0,0,20" 
                Content="Connect" FontSize="20" Command="{Binding ConnectCommand}">
            
        </Button>
        
    </Grid>
</UserControl>
